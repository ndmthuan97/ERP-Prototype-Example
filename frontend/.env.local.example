# ============================================================
# FE env — copy thành .env.local rồi chỉnh nếu service đổi port
# ============================================================
# FE gọi THẲNG 3 service (chưa có api-gateway). Khi gateway ra đời,
# chỉ cần trỏ cả 3 về 1 URL gateway — không sửa code.
NEXT_PUBLIC_CUSTOMER_API=http://localhost:3001
NEXT_PUBLIC_SALES_API=http://localhost:3002
NEXT_PUBLIC_INVENTORY_API=http://localhost:3003
